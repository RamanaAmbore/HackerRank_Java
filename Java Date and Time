    public static String findDay(int month, int day, int year) {
        Calendar cl = Calendar.getInstance();
        cl.set(year, month-1, day);
        return(cl.getDisplayName(Calendar.DAY_OF_WEEK, Calendar.LONG, Locale.US).toUpperCase());

    }
