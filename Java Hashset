
        //Write your code here
        HashSet<String> hs = new HashSet<>();
        for (int i = 0; i < t; i++) {
            hs.add(pair_left[i]+" "+pair_right[i]);
            System.out.printf("%d\n",hs.size());
        }
